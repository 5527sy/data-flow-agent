# DataFlow Agent 依赖（按里程碑逐步启用）
# ── 应用层（M1）──
fastapi>=0.115
uvicorn[standard]>=0.30
openai>=1.40            # DeepSeek（OpenAI 兼容协议）
pydantic>=2.9           # 结构化输出"合同"
PyYAML>=6.0
jinja2>=3.1             # 卡片/提示词模板
typer>=0.12
rich>=13.7
tenacity>=8.5           # 重试
loguru>=0.7
requests>=2.31

# ── 执行层（M2/M3 启用）──
# pandas>=2.2
# scikit-learn>=1.5
# lightgbm>=4.5
# xgboost>=2.1
# toad>=0.1.1            # WOE/IV 分箱
# shap>=0.46
# openpyxl>=3.1
# xlsxwriter>=3.2
